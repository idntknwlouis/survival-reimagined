# Gradle
org.gradle.jvmargs=-Xmx2G
org.gradle.parallel=true
org.gradle.configuration-cache=false

# Fabric / Minecraft
minecraft_version=1.21.1
loader_version=0.19.5
loom_version=1.18.2

# Survival Reimagined
mod_version=1.4.0-BETA-1
maven_group=net.mcreator
archives_base_name=survival-reimagined-fabric

# Dependencies
fabric_api_version=0.116.17+1.21.1
mixinextras_version=0.5.3
jei_version=19.21.0.246

# Optional C2ME compatibility test runtime
# OpenCL acceleration itself requires launching Gradle/Minecraft on Java 25.
enable_c2me_runtime=false
c2me_version_id=4kQdubQA
c2me_opencl_curse_file=8896919
scalablelux_version_id=AodeMVa6

# Optional: pin one OpenCL device when the same GPU is exposed by multiple drivers.
# Leave blank for normal C2ME behavior. This value is only used when enable_c2me_runtime=true.
c2me_opencl_device_uuid=
